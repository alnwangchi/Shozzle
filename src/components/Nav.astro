---
// library imports
import { Icon } from 'astro-icon';

// component imports
import Hamburger from '@components/Hamburger.astro';

// data imports
import navData from '@constants/navData';
---

<nav class="navbar">
  <div class="logo">
    <a href="/"> shozzle</a>
  </div>
  <ul class="hidden lg:flex">
    {
      navData.map((item) => (
        <li class=" text-secondary hover:text-primary border-solid border-soft border-l">
          <a href={item.path} class="h-20 pt-[2.5rem] pb-1 pr-8 pl-36 flex" data-navLink>
            {item.name}
          </a>
        </li>
      ))
    }
    <li class="relative w-[180px] border-solid border-soft border-l">
      <div id="sun-moon"></div>
    </li>
  </ul>
  <Hamburger />
</nav>

<script></script>
